<div class="well">
  <%= simple_form_for(@invoice) do |f| %>
    <%= f.error_notification %>

    <div class="form-inputs">
      <%= f.association :client, collection: current_user.account.clients.all(order: 'first_name'), label_method: :full_name, :required => true %>
      <%= f.input :american_start_date, label: 'Start Date', input_html: { id: 'start_date' }, :required => true %>
      <%= f.input :american_end_date, label: 'End Date', input_html: { id: 'end_date' }, :required => true %>
      <%# f.input :american_due_date, input_html: { id: 'due_date' } %>
      <%= f.input :paid %>
      <%= f.hidden_field :account_id, :value => current_user.account.id %>
    </div>
  
    <div class="form-actions">
      <%= f.button :submit %>
    </div>
  <% end %>
</div>